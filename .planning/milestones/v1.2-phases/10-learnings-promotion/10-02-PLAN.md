---
phase: 10-learnings-promotion
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - ~/.agent-deck/conductor/LEARNINGS.md
  - ~/.agent-deck/conductor/agent-deck/LEARNINGS.md
  - ~/.agent-deck/conductor/ard/LEARNINGS.md
  - ~/.agent-deck/conductor/opengraphdb/LEARNINGS.md
  - ~/.agent-deck/conductor/ryan/LEARNINGS.md
autonomous: true
requirements: [LEARN-04]

must_haves:
  truths:
    - "Every LEARNINGS.md entry that was promoted in Plan 01 is marked with Status: promoted and a cross-reference to its destination"
    - "Entries already marked Status: retired are removed entirely from their source files"
    - "Duplicate entries across files are consolidated: most detailed version stays, others reference it"
    - "Template headers and entry format sections in all LEARNINGS.md files remain intact"
    - "Empty LEARNINGS.md files (si, work) are untouched"
  artifacts:
    - path: "~/.agent-deck/conductor/LEARNINGS.md"
      provides: "Root file with 4 entries marked promoted"
      contains: "promoted"
    - path: "~/.agent-deck/conductor/agent-deck/LEARNINGS.md"
      provides: "Agent-deck file with promoted/project-specific entries annotated"
      contains: "promoted"
    - path: "~/.agent-deck/conductor/ard/LEARNINGS.md"
      provides: "ARD file with promoted entries marked, retired entries removed, project-specific entries unchanged"
      contains: "promoted"
    - path: "~/.agent-deck/conductor/opengraphdb/LEARNINGS.md"
      provides: "Opengraphdb file with promoted entries annotated, project-specific entries unchanged"
      contains: "promoted"
    - path: "~/.agent-deck/conductor/ryan/LEARNINGS.md"
      provides: "Ryan file with promoted entries marked, project-specific entries unchanged"
      contains: "promoted"
  key_links:
    - from: "10-01-SUMMARY.md"
      to: "LEARNINGS.md source files"
      via: "Cross-reference to know which entries were promoted where"
      pattern: "Status: promoted"
---

<objective>
Clean up all seven LEARNINGS.md source files after promotion: mark promoted entries with "Status: promoted" and their destination, remove retired entries, and consolidate duplicates with cross-references.

Purpose: Transform LEARNINGS.md files from accumulating archives into living journals where only active, non-promoted entries remain as actionable items.
Output: Seven cleaned-up LEARNINGS.md files with clear status tracking on every entry.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-learnings-promotion/10-RESEARCH.md

# Need Plan 01 summary to know exactly what was promoted where
@.planning/phases/10-learnings-promotion/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mark promoted entries and remove retired entries in all LEARNINGS.md files</name>
  <files>
~/.agent-deck/conductor/LEARNINGS.md
~/.agent-deck/conductor/agent-deck/LEARNINGS.md
~/.agent-deck/conductor/ard/LEARNINGS.md
~/.agent-deck/conductor/opengraphdb/LEARNINGS.md
~/.agent-deck/conductor/ryan/LEARNINGS.md
  </files>
  <action>
Read all 7 LEARNINGS.md files in full. Read the 10-01-SUMMARY.md to confirm what was promoted and where. Then for each file:

**For entries that were promoted in Plan 01:**
Add `Status: promoted` and `Promoted to: {destination file}` to the entry metadata. Do NOT remove the entry content, just add the status annotation. This preserves the learning's history while signaling it lives in a shared location now.

For files using the standard `[YYYYMMDD-NNN]` format (root, agent-deck, ard, ryan), add the status line in the entry's metadata block:
```
Status: promoted
Promoted to: conductor CLAUDE.md / gsd-conductor SKILL.md / agent-deck-workflow SKILL.md
```

For the opengraphdb file (uses section header format instead of entry IDs), add an inline note at the end of promoted sections:
```
> Status: promoted to {destination}
```

**For entries already marked Status: retired** (ard 004 and ard 006):
Remove these entries entirely from the file. They are historical artifacts that are no longer relevant.

**For duplicate entries across files:**
Keep the most detailed version with its promoted status. For the less-detailed duplicates, mark them with:
```
Status: promoted (consolidated)
See: {file with canonical version}
```

Key duplicates to consolidate (from RESEARCH.md inventory):
- root 003 and agent-deck 001 (GSD auto-advance): keep agent-deck 001 (more detail)
- root 004 and agent-deck 002 (Enter key): keep root 004 (original discovery)
- ard 008 and root 002 (parent linkage): keep root 002 (canonical)
- ard 010 and ryan 011 (sleep+poll): keep ard 010 (more detail)
- ryan 014 and root 001 (open-ended prompts): keep root 001 (original)

**For project-specific entries** (stays in their file):
Leave completely untouched. Do not add any status annotation.

**For empty template files** (si, work):
Leave completely untouched. Do not modify.

**Template preservation rule:** Keep the "How to Use This File" header, entry format template, and any section dividers intact in every file. Only modify entries below the content separator.
  </action>
  <verify>
    <automated>echo "=== Promoted counts ===" && grep -c "promoted" ~/.agent-deck/conductor/LEARNINGS.md ~/.agent-deck/conductor/agent-deck/LEARNINGS.md ~/.agent-deck/conductor/ard/LEARNINGS.md ~/.agent-deck/conductor/opengraphdb/LEARNINGS.md ~/.agent-deck/conductor/ryan/LEARNINGS.md 2>/dev/null && echo "=== Retired check (should be 0) ===" && grep -c "retired" ~/.agent-deck/conductor/ard/LEARNINGS.md && echo "=== Empty files unchanged ===" && wc -l ~/.agent-deck/conductor/si/LEARNINGS.md ~/.agent-deck/conductor/work/LEARNINGS.md</automated>
  </verify>
  <done>All 5 active LEARNINGS.md files have promoted entries marked with Status: promoted and destination cross-reference. Retired entries (ard 004, 006) removed. Duplicate entries consolidated with cross-references. Template headers intact. Empty files (si, work) unchanged at 21 lines each. Project-specific entries untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Validate completeness across all source and destination files</name>
  <files>~/.agent-deck/conductor/CLAUDE.md, ~/.agent-deck/skills/pool/gsd-conductor/SKILL.md, ~/.agent-deck/skills/pool/agent-deck-workflow/SKILL.md</files>
  <action>
Read all 3 destination files and all 7 source files. Perform a completeness audit:

1. For every entry classified as "Universal" in the RESEARCH.md inventory, verify it appears in conductor CLAUDE.md.
2. For every entry classified as "GSD" in the RESEARCH.md inventory, verify it appears in gsd-conductor SKILL.md.
3. For every entry classified as "Agent-deck op" in the RESEARCH.md inventory, verify it appears in agent-deck-workflow SKILL.md.
4. For every promoted entry in source files, verify the Status: promoted annotation exists.
5. Verify no project-specific entries were accidentally promoted to shared files.
6. Verify exit 137 sections in CLAUDE.md and gsd-conductor SKILL.md were not modified (compare section structure with known state from Phase 9).

If any gaps found, fix them. If all checks pass, report the audit results.

Output the audit as a summary table showing: total entries audited, promoted count, project-specific count (unchanged), retired count (removed), empty files (skipped).
  </action>
  <verify>
    <automated>echo "=== Destination files have content ===" && grep -l "Orchestration Best Practices" ~/.agent-deck/conductor/CLAUDE.md && grep -l "Operational Patterns" ~/.agent-deck/skills/pool/agent-deck-workflow/SKILL.md && echo "=== No project-specific leaks ===" && grep -ic "ElevenLabs\|React 19\|peer.dep\|deploy.*ALL.*containers\|bump.*ALL.*package" ~/.agent-deck/conductor/CLAUDE.md ~/.agent-deck/skills/pool/gsd-conductor/SKILL.md ~/.agent-deck/skills/pool/agent-deck-workflow/SKILL.md; echo "exit: $?"</automated>
  </verify>
  <done>All entries accounted for: universals in CLAUDE.md, GSD in SKILL.md, agent-deck ops in workflow SKILL.md, project-specific unchanged in source files. No leaks of project-specific content into shared files. Exit 137 sections intact. Audit table produced.</done>
</task>

</tasks>

<verification>
Final phase verification checklist:

1. Every universal pattern from RESEARCH.md Classification Summary appears in conductor CLAUDE.md
2. Every GSD learning from RESEARCH.md appears in gsd-conductor SKILL.md
3. Every agent-deck op learning from RESEARCH.md appears in agent-deck-workflow SKILL.md
4. All promoted entries in source files have "Status: promoted" annotation
5. Retired entries (ard 004, 006) are removed from ard LEARNINGS.md
6. Empty files (si, work) are unchanged at 21 lines
7. No project-specific entries in shared destination files
8. Exit 137 sections untouched in CLAUDE.md and gsd-conductor SKILL.md
</verification>

<success_criteria>
- All 5 active LEARNINGS.md files have promoted entries annotated
- 2 retired entries removed from ard LEARNINGS.md
- Duplicates consolidated with cross-references
- Template structure preserved in all files
- Empty files (si, work) untouched
- Completeness audit passes with zero gaps
</success_criteria>

<output>
After completion, create `.planning/phases/10-learnings-promotion/10-02-SUMMARY.md`
</output>
